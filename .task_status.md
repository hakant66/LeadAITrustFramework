# Task Status

STATE: READY_FOR_REVIEW

## Summary

Implemented **structured Next Steps (JSON)** for the board-level report as specified in the plan "LLM and AI Models for a More Intelligent LeadAI":

- **Backend (core-svc):** Added `BoardLevelNextStepItem` model and `next_steps` to `BoardLevelReportResp`. Parse Next Steps table from report markdown via `_parse_next_steps_from_report()`; when manual steps are used, `_manual_steps_to_next_steps()` provides the same shape. Both cache-hit and fresh-report responses now include `next_steps` for UI and automation (e.g. Jira).
- **Frontend (web):** Board-level report page uses `report.next_steps` from the API when present, with fallback to client-side parsing. Added **Export next steps (JSON)** button (client component `BoardLevelNextStepsExport`) to copy structured payload (entity_name, generated_at, next_steps) to clipboard for use in Jira or other automation.

## Files changed

- `apps/core-svc/app/routers/ai_reports.py` — response model, parser, manual-step conversion, both return paths
- `apps/web/src/app/scorecard/admin/governance-dashboard-reporting/BoardLevelReportPage.tsx` — type, use API next_steps, export button
- `apps/web/src/app/scorecard/admin/governance-dashboard-reporting/BoardLevelNextStepsExport.tsx` — new client component for export
